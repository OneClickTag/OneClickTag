{
  "version": 2,
  "buildCommand": "pnpm prisma generate --schema=./prisma/schema.prisma && pnpm run build",
  "outputDirectory": ".",
  "functions": {
    "dist/main.js": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/dist/main.js"
    }
  ]
}
